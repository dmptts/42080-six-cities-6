import React from 'react';

const LoadingScreen = () => {
  return (
    <p style={{
      width: `100%`,
      marginTop: `50px`,
      fontSize: `32px`,
      textAlign: `center`
    }}>Loading ...</p>
  );
};

export default LoadingScreen;
